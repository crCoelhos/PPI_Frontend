import React, { FC } from 'react';
import styles from './CustomerEditTaskForm.module.css';

interface CustomerEditTaskFormProps {}

const CustomerEditTaskForm: FC<CustomerEditTaskFormProps> = () => (
  <div className={styles.CustomerEditTaskForm}>
    CustomerEditTaskForm Component
  </div>
);

export default CustomerEditTaskForm;
