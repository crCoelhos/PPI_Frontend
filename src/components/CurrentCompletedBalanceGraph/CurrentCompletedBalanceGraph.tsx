import React, { FC } from 'react';
import styles from './CurrentCompletedBalanceGraph.module.css';

interface CurrentCompletedBalanceGraphProps {}

const CurrentCompletedBalanceGraph: FC<CurrentCompletedBalanceGraphProps> = () => (
  <div className={styles.CurrentCompletedBalanceGraph}>
    CurrentCompletedBalanceGraph Component
  </div>
);

export default CurrentCompletedBalanceGraph;
